/*
import { useThree } from "@react-three/fiber";
import { useEffect } from "react";
import * as THREE from 'three'

const Background = () => {
  const { scene } = useThree();

  useEffect(() => {
    scene.background = new THREE.Color("#ffffff"); // Cor de fundo
  }, [scene]);

  return null;
};

export default Background;
*/