import styled, { CSSProperties } from "styled-components";

export const InputContentStyle = styled.main<CSSProperties>`
  * {
    font-family: inter, Arial, Helvetica, sans-serif;
  }

  display: flex;
  flex-direction: column;
  align-items: start;
  justify-content: left;
  padding: 20px 0px;
  

`;