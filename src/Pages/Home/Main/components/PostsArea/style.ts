import styled, { CSSProperties } from "styled-components";

export const ContentStyle = styled.div<CSSProperties>`
    div.box {
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px 40px;
    }
`;